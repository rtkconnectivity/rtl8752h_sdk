##########################################################################################################################
# File automatically-generated by tool: [projectgenerator] version: [3.5.2] date: [Fri Nov 29 15:56:19 CST 2019]
##########################################################################################################################
# ------------------------------------------------
# Generic Makefile (based on gcc)
#
# ChangeLog :
# 2019-02-01 - first version
# 2023-12-05 - second version
# ------------------------------------------------

######################################
# target
######################################
TARGET = app

######################################
# building variables
######################################
# debug build?
DEBUG = 0
# optimization
OPT = -O1

#######################################
# paths
#######################################
# Build path
BUILD_DIR = build
BIN_DIR = bin

######################################
# source
######################################
# C sources
C_SOURCES =  \
../../../../src/mcu/rtl876x/system_rtl876x.c \
../../../../src/mcu/peripheral/rtl876x_nvic.c \
../../../../src/mcu/peripheral/rtl876x_io_dlps.c \
../../../../src/app/ppt_driver/common/ppt_driver.c \
../../../../src/app/ppt_driver/simple/ppt_simple.c \
../template/../../../../src/mcu/rtl876x/overlay_mgr.c \
../../../../src/app/ppt_simple_trx/main.c \
../../../../src/app/ppt_simple_trx/app_task.c \
../../../../src/app/ppt_simple_trx/ppt_cfg.c \
../../../../src/app/ppt_simple_trx/ppt_prx.c \
# C sources END

# ASM sources
ASM_SOURCES = ../../../../src/mcu/rtl876x/arm/startup_rtl876x_gcc.s
# ASM sources END

#######################################
# binaries
#######################################
PREFIX = arm-none-eabi-
# The gcc compiler bin path can be either defined in make command via GCC_PATH variable (> make GCC_PATH=xxx)
# either it can be added to the PATH environment variable.
ifdef GCC_PATH
CC = $(GCC_PATH)/$(PREFIX)gcc
AS = $(GCC_PATH)/$(PREFIX)gcc -x assembler-with-cpp
CP = $(GCC_PATH)/$(PREFIX)objcopy
SZ = $(GCC_PATH)/$(PREFIX)size
else
CC = $(PREFIX)gcc
AS = $(PREFIX)gcc -x assembler-with-cpp
CP = $(PREFIX)objcopy
SZ = $(PREFIX)size
OD = $(PREFIX)objdump
endif
HEX = $(CP) -O ihex
BIN = $(CP) -O binary -S

#######################################
# CFLAGS
#######################################
# cpu
CPU = -mcpu=cortex-m0plus

# fpu
FPU =

# float-abi
FLOAT-ABI = -mfloat-abi=soft

# mcu
MCU = $(CPU) -mthumb  $(FLOAT-ABI)

# MACROS for gcc
# AS Defines
AS_DEFS =
# AS DDefines END

# C Defines
C_DEFS =
# C Defines END

# AS Includes
AS_INCLUDES = \
# AS Includes END

# C Includes
C_INCLUDES =  \
-I../../../../inc/app \
-I../../../../inc/bluetooth/gap \
-I../../../../inc/bluetooth/profile/client \
-I../../../../inc/bluetooth/profile/server \
-I../../../../inc/bluetooth/profile \
-I../../../../inc/os \
-I../../../../inc/peripheral \
-I../../../../inc/platform \
-I../../../../src/app/ppt_driver/common \
-I../../../../src/app/ppt_driver/simple \
-I../../../../src/app/ppt_simple_trx \
-I../ \
# C Includes END

# C Pre Includes
PRE_INCLUDE=  \
-include app_define.h \
-include mem_config.h \
# C Pre Includes END

# C Pre Defines
PRE_DEFINE=  \
# C Pre Defines END

# compile gcc flags
ASFLAGS = $(MCU) $(AS_DEFS) $(AS_INCLUDES) $(OPT) -Wall -fdata-sections -ffunction-sections

CFLAGS = $(MCU) -specs=nano.specs $(C_DEFS) $(C_INCLUDES) $(OPT) -Wall -fdata-sections -ffunction-sections

ifeq ($(DEBUG), 1)
CFLAGS += -g -gdwarf-2
endif

CFLAGS += -std=c99 
# Generate dependency information
CFLAGS += -MMD -MP -MF"$(@:%.o=%.d)"

# pre-include 
ifneq ($(PRE_INCLUDE), )
CFLAGS +=$(PRE_INCLUDE)
endif
ifneq ($(PRE_DEFINE), )
CFLAGS +=$(PRE_DEFINE)
endif

#######################################
# LDFLAGS
#######################################
# link script
LDSCRIPT = app.ld.gen

LIBS = -lc -lm -lnosys

# Libraries
LIBDIR = \
../../../../bin/gcc/libROM.a \
../../../../bin/gcc/liblowerstack.a \
../../../../bin/gcc/librtl8752h_sdk.a \
# Libraries END

LDFLAGS = $(MCU) -T$(LDSCRIPT) $(LIBDIR) $(LIBS) -Wl,-Map=$(BUILD_DIR)/$(TARGET).map,--cref -Wl,--gc-sections  -specs=nano.specs

# default action: build all
.PHONY : all rm_app_bin
all:rm_app_bin $(BUILD_DIR)/$(TARGET).elf $(BIN_DIR)/$(TARGET).bin $(BIN_DIR)/$(TARGET).trace 
	../../../../tool/Gadgets/prepend_header/prepend_header.exe -b 16 -t app_code -p bin/$(TARGET).bin -m 1 -i "..\mp.ini" -c sha256 -a "../../../../tool/Gadgets/key.json" -r "../../../../tool/Gadgets/rtk_rsa.pem"  --aesmode CTR
	../../../../tool/Gadgets/md5/md5.exe bin/$(TARGET)_MP.bin
# Default action: Build all targets.
#
# Optional: Generate a disassembly file for debugging.
# To enable this feature, remove the leading '#' from the command: $(OD) -D -S build/$(TARGET).elf > bin/$(TARGET).dis
# Note:
#   The '-S' flag interleaves source code with disassembled output, which is useful
#   for correlating the source with the corresponding machine code. However, since the
#   output file will contain source code, ensure that you protect its confidentiality.
    #	$(OD) -D -S build/$(TARGET).elf > bin/$(TARGET).dis

# Remove the old app.bin to allow MAKE to regenerate app.bin from the elf file, 
# ensuring that the app.bin passed to the prepend_header script is plain-text.
rm_app_bin:
	rm -rf $(BIN_DIR)/$(TARGET).bin
#######################################
# build the application
#######################################
# list of objects
OBJECTS = $(addprefix $(BUILD_DIR)/,$(notdir $(C_SOURCES:.c=.o)))
vpath %.c  $(dir $(C_SOURCES))

# list of ASM program objects
OBJECTS += $(addprefix $(BUILD_DIR)/,$(notdir $(ASM_SOURCES:.s=.o)))
vpath %.s $(sort $(dir $(ASM_SOURCES)))

$(BUILD_DIR)/%.o: %.c Makefile | $(BUILD_DIR) 
	$(CC) -c $(CFLAGS) -Wa,-a,-ad,-alms=$(BUILD_DIR)/$(notdir $(<:.c=.lst)) $< -o $@

$(BUILD_DIR)/%.o: %.s Makefile | $(BUILD_DIR)
	$(AS) -c $(ASFLAGS) $< -o $@

$(LDSCRIPT):
	$(CC) -E -P -x c $(C_INCLUDES) $(basename $@) -o $@

$(BUILD_DIR)/$(TARGET).elf: $(OBJECTS) Makefile | $(LDSCRIPT)
	$(CC) $(OBJECTS) $(LDFLAGS) -o $@
	$(SZ) $@

$(BUILD_DIR)/%.hex: $(BUILD_DIR)/%.elf | $(BUILD_DIR)
	$(HEX) $< $@

$(BIN_DIR)/%.bin: $(BUILD_DIR)/%.elf | $(BIN_DIR)
	$(BIN) $< $@ --remove-section=App.trace

$(BIN_DIR)/%.trace: $(BUILD_DIR)/%.elf | $(BIN_DIR)
	$(BIN) $< $@  --only-section=App.trace

$(BUILD_DIR):
	mkdir -p $@

$(BIN_DIR):
	mkdir -p $@

#######################################
# clean up
#######################################
clean:
	-rm -fR $(BUILD_DIR)
	-rm -fR $(BIN_DIR)
	-rm -f $(LDSCRIPT)

#######################################
# dependencies
#######################################
-include $(wildcard $(BUILD_DIR)/*.d)

# *** EOF ***